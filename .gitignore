# Environments
.env
.venv/
venv/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Raw SEC filing HTML — scratch space, not needed after parsing into SQLite
data/sec_filings/

# SQLite WAL/SHM side-files — not needed in git (main .db is committed)
data/*.db-shm
data/*.db-wal

# Large scratch data
*.pkl.tmp

.pytest_cache/
.coverage

# Streamlit local secrets (use platform secrets on deploy)
.streamlit/secrets.toml
